services:
  postgres:
    image: 'postgres:latest'
    container_name: 'postgres_elo-vet-api'
    environment:
      - 'POSTGRES_DB='
      - 'POSTGRES_PASSWORD='
      - 'POSTGRES_USER='
    ports:
      - '5432:5432'